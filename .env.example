# OpsVale environment configuration
# Copy to .env.local for local development.

# APP_URL: Public base URL of the app. Used for self-referential links.
APP_URL="http://localhost:3000"

# Database, auth, and email credentials are introduced in later phases
# (Phase 1+). Add them here as they are needed.
